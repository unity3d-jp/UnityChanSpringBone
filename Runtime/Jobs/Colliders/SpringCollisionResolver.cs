using System;
using UnityEngine;

namespace Unity.Animations.SpringBones.Jobs
{
    public static partial class SpringCollisionResolver
    {
        public enum Axis
        {
            X = 0,
            Y,
            Z, 
            AxisCount
        }
    }
}